test_angle_encoding_rz_001 ← {
    ⍝ The sum of the squares of the vector's entries must be equal to 1 (This test is failing)!!!
    res ← 'rz' angle_encoding (1 2 3 4) ⍝ Generate the quantum vector state
    norm_states ← ⊃¨ ( {+/⍉ ⊃ (res*2)} ¨res ) ⍝ Get the norm    
    'Test: Verify that the sum of the squares of the vectors entries must be equal to 1' ⊢ 1 1 1 1 Assert norm_states:
    ''
}